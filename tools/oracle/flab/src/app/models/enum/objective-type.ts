// VENDOR PATCH: dropped primeng SelectItem import + objectiveTypeOptions (UI only).
export enum ObjectiveType {
  Output = 0,
  Input = 1,
  Maximize = 2,
  Limit = 3,
  HideSep = 10,
  ItemLimit = 11,
  ItemLimitOutput = 12,
  MachineLimit = 13,
  DomainTransfer = 14,
}
